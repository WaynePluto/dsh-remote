# 0.0.2-20260922（2026-09-22）

预发布版本：在 0.0.1 之后的三轮改进之上，发行包分 core / full 两种变体。
版本号带日期后缀，GitHub Release 会标记为预发布。

## 发行包分 core / full

- 每个平台各提供 core 与 full 两个 zip，没有默认包，按需下载
- 两者是同一个程序：core 剔除了 Office 文档预览的 LibreOffice 转换引擎，
  Windows 包从 184 MB 回到 67 MB（Linux 63 MB、macOS 63 MB）
- core 打开 Office 文档预览会提示转换不可用，dsh 启动与其余功能不受影响；
  需要该功能就下载 full

## 功能插件可管理、可卸载

- 20 个功能组件按用途组成 4 个组合包与 6 个独立第三方 Bundle，首次启动默认安装并启用
- 后续升级 dsh-station 会配套升级仍安装的插件，并保留 Bundle 与组件的停用状态；卸载后不自动补回
- 发行包根目录提供 `plugins/` 安装介质，Windows 托盘可直接打开该目录；需要恢复时通过 dsh
  的「添加插件」选择对应目录
- 启动时把插件介质和运行依赖缓存到 profile 所在磁盘，并自动迁移不同 pnpm 主版本创建的
  `node_modules`，避免 Windows 跨盘链接或 store 版本变化导致启动直接退出
- remote-privileged 拆分：隧道连接所需的注入仍是不可停用的壳级补丁；远程设置进入远程体验组合包
- 修复组合 Bundle、简洁模式与固定 YOLO 在线停用时的 HMR 残留行；模型启动屏障在进程内保持稳定，10 个随附 Bundle 均可在线停用并重新启用
- 网页目录选择因需要整体覆盖原生选择器而独立分发；固定 YOLO 与简洁模式也保持独立 Bundle

## 升级 dsh 0.1.6-alpha.2

- 内嵌 dsh 从 0.1.5-rc.2 升级到 0.1.6-alpha.2（上游 alpha 版本），随之带入
  Office 文档转 PDF 预览（即 full 变体携带的引擎）
- 适配上游接口变化：常用模型收藏、用户消息分叉、文件侧栏入口、
  旧 WebKit 浏览器兼容
- 术语统一：用户文案「精简模式」改为「简洁模式」，与预设界面一致

## 界面与体验

- services 日志弹窗与 turn-retry 失败详情弹窗支持全屏切换，全屏时隐藏拖动与调整手柄
- services 常驻服务列表单行省略，长服务名不再把行尾按钮挤换行
- Windows 托盘：双击启动在 dsh 就绪后弹「已启动」系统通知；
  开机自启动改为静默登录，旧自启条目自动升级

## 已知限制

- 内嵌 dsh 为上游 0.1.6-alpha.2，API 仍可能变化
- 部分插件功能待实机验收（详见仓库 roadmap）
