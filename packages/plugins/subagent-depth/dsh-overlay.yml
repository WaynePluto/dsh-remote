# dsh-remote 的 dsh 插件叠加层。launcher 会把它作为 --patch 传给 dsh。
# Host 半注册设置命名空间与全局子代理深度 guard；浏览器半通过
# package.json 的 dsh.client + exports["./client"] 进入设置插件卡片。
- insert:
    - id: subagent-depth
      name: './dist/index.js'
